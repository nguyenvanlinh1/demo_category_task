import { Box, LinearProgress } from "@mui/material";

const CustomProgress = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <LinearProgress />
    </Box>
  );
};

export default CustomProgress;
